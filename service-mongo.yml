apiVersion: v1
kind: Service
metadata:
  name: my-mongodb-service-test
  namespace: eval
spec:
  selector:
    app: my-mongodb
  ports:
  - port: 27017
    targetPort: 27017 
    nodePort: 32003
    protocol: TCP
  type: NodePort